{
  "name": "desktopagent",
  "version": "2.0.0",
  "description": "Desktop Agent (Phase 6 - CDP Implementation)",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-extension": "echo \"Building CDP extension...\" && cp -r webx-extension/* dist/",
    "install-extension": "echo \"Install Chrome extension from dist/ directory\""
  },
  "keywords": ["automation", "cdp", "chrome-extension", "desktop-agent"],
  "author": "Desktop Agent Team",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
