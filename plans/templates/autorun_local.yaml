dsl_version: "1.1"
name: "L4 Autorun Local Test"
description: "Non-destructive plan to trigger L4 autorun via policy guard"
variables:
  form_url: "http://localhost:8000/mock/form"
steps:
  - open_browser:
      url: "{{form_url}}"
      context: "autorun_session"
  - log:
      message: "Opened local mock form"
